This .firebase directory stores local Firebase config.
The actual Firebase project "T1D-heal" (display name: "T1D Saathi")
must be created via:

  firebase projects:create T1D-heal --display-name "T1D Saathi"

Then run:
  firebase use T1D-heal
